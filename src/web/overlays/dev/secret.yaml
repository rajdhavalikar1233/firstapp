apiVersion: v1
kind: Secret
metadata:
  name: secret
  labels:
    app: v1
  annotations:
    avp.kubernetes.io/path: "secrets/data/web/dev"
    avp.kubernetes.io/secret-version: "1"    
stringData:
  KEY: <KEY>
